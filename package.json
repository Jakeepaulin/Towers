{
  "name": "towers",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd Towers.client && npm i",
    "setup:server": "cd Towers && npm i",
    "start": "node Towers/index.js"
  },
  "engines": {
    "node": "^16"
  }
}
